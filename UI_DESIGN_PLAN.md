# 新月罗盘 UI 与视觉升级方案

日期：2026-09-10  
状态：实施中。2026-09-10 已完成第一轮 UI 改造；原生地图集成及更深层的服务拆分仍为后续事项。  
适用项目：CrescentCompass（新月罗盘）独立 Dalamud 插件。

### 当前实施记录

第一轮已经落地：统一深色主题、紧凑／展开地图布局、寻宝／事件／路线详情页、地图缩放工具条、真实路线图层、分类设置导航、标记尺寸预览、水晶与 CE／FATE 等独立倍率、标记透明度、减少动态效果、候选悬停放大、CE 空结果处理、场景标签避让，以及未知高差的真实显示。

后续调整已将内嵌详情改为独立吸附卡片：地图保持紧凑尺寸，卡片根据屏幕空间吸附在右侧、左侧或上下方，并允许用户分离后独立移动。水晶流程也改为在提交传送或交互前，先通过通用动作确认结束坐骑／上坐骑状态。

本轮 Release 构建通过且现有 18 组核心逻辑测试通过。游戏内的视觉尺寸、点击命中、传送、坐骑和键盘取消仍需使用实际客户端复测；这类行为不能由编译或纯逻辑测试代替。

## 1. 设计目标与边界

将新月罗盘设计为一个现代、紧凑、适合游戏中持续使用的地图工具：地图始终是主体，当前目标一眼可见，复杂信息按需展开。

视觉方向为：**游戏地图原有纹理、深色工具面板、金色关注标记、青色候选与路线、克制的交互动效。**

必须遵守以下边界：

- 保持 DR 版本的候选计算、提示处理、路线计算与定制路线规则；美化不能顺带改变业务语义。
- 水晶传送、CE／FATE 导航、传送后上坐骑、WASD／Esc 取消等功能需要独立回归，界面变化不能视为功能修复证明。
- 不恢复地图上常驻的“检测到手动移动，自动导航已取消”等提示行。
- 传送标记和 CE／FATE 标记独立调节大小；允许用户通过预览自行校准。
- 未知高差必须显示为未知，不得以回退值伪装成 `0m`。
- 本方案不引入 DR 或 OmenTools 运行时依赖。
- 将功能附加到游戏实际原生地图窗口，仅作为远期讨论项，不属于本轮实施授权。

本文中的尺寸、颜色和时长均为初始设计值，最终以同一 UI 缩放、窗口尺寸和场景下的游戏内对照为准。

## 2. 界面结构

### 2.1 主地图窗口

提供两种布局，共用同一份数据和交互逻辑：

| 模式 | 用途 | 内容 |
| --- | --- | --- |
| 紧凑模式，默认 | 游戏中常驻 | 标题、实例／来源、简短预测摘要、地图、必要地图操作 |
| 展开模式 | 查询事件与规划路线 | 左侧地图，右侧可折叠详情栏：寻宝／事件／路线 |

建议紧凑窗口初始宽度约 320–360 逻辑像素；展开窗口约 640–760 逻辑像素。布局应根据可用内容宽度决定是否并排，不以固定屏幕分辨率判断。空间不足时详情页切换显示，不强行压缩地图和文字。

```text
┌────────────────────────────────────────────────────────┐
│ 新月罗盘 · 南部                       锁定  设置  关闭 │
│ 实例 3810085 · 本机观察                                 │
├────────────────────────────┬───────────────────────────┤
│ 幸福的魔法罐（上）         │ 寻宝    事件    路线      │
│ 预计 04:26 · 剩余 03:42    │                           │
│                            │ 剩余候选                  │
│                            │ 03                        │
│        游戏地图纹理        │                           │
│                            │ 当前关注 #60              │
│      图标、候选、路线      │ 312m · ↑18m               │
│                            │                           │
│                            │ 提示记录                  │
│                            │ 西北 · 距离较近           │
│                            │                           │
│ 图层          −  复位  ＋ │ 导航到关注点      停止    │
└────────────────────────────┴───────────────────────────┘
```

示意中的实例、时间、距离和高差均为样例。侧栏支持折叠；独立详情窗口可作为后续选项，避免首轮同时维护过多布局。

### 2.2 标题与状态区

- 标题只显示插件名与区域；锁定、设置和关闭提供悬停说明。
- 实例、预测时间、记录来源使用独立字段布局。长名称省略，悬停显示全文。
- 预测摘要最多两行，正常数据刷新不能反复改变地图位置。
- 过期数据显示“等待新记录”，悬停说明上次记录时间、来源与失效原因。
- 只有实际查询进行中才显示加载指示，不以无限转圈表达“暂无数据”。
- 紧凑模式保留“CE 关注／触发怪”入口；展开模式由事件页承接。
- 导航进行时显示小型活动标识及明确的“停止”操作；取消后恢复静态界面。
- 详细状态和失败原因放入路线页或按需展开的消息区。严重、需要用户处理的错误可短暂提示，并保留可查询记录。

### 2.3 地图视口

- 地图内容保持正确比例，不随窗口形状拉伸。
- 缩放以鼠标所在地图位置为中心；拖动只在地图交互区域生效。
- 调整窗口大小时保留当前世界坐标中心；“复位”明确恢复完整地图视图。
- 图层入口统一管理候选、事件、路线、队友和警戒范围。
- 浮动工具条放在固定边角，不遮挡核心标记；工具条优先接收自己的输入。
- 锁定位置与禁止地图拖动分开设置，避免同一个锁图标含义不清。

## 3. 视觉规范

### 3.1 颜色与层级

| 用途 | 初始值 | 使用说明 |
| --- | --- | --- |
| 主面板 | `#202633`，默认 92% 不透明 | 保持游戏背景可感知，同时保证可读性 |
| 次级面板 | `#293243` | 详情分组、选中区域 |
| 正文 | `#E8EDF4` | 名称、主要数值 |
| 辅助文字 | `#A9B3C3` | 来源、坐标、说明 |
| 关注／选中 | `#FFB728` | 当前目标、关注候选 |
| 普通候选／路线 | `#5AD6E6` | 与金色关注形成区分 |
| 已确认宝箱 | `#75D858` | 必须同时有独立图形或标签 |
| 异常／冲突 | `#F08070` | 配合文字与图形，不只靠颜色 |

面板背景、地图纹理、标记分别控制透明度。降低面板透明度不应同时使图标和文字变淡。

### 3.2 尺寸、字体与控件

- 间距统一采用 4／8／12／16 逻辑像素。
- 面板圆角约 8，按钮约 5，状态标签约 3 逻辑像素。
- 采用 Dalamud 可用的中文字体，区分标题、正文、辅助文字三个层级。
- 距离、倒计时和剩余数量适度突出，不让整块面板都使用高亮色。
- 默认紧凑密度，可选择舒适密度；不通过缩小中文字体强行塞入内容。
- 所有逻辑尺寸仅应用一次全局 UI 缩放，防止重复缩放。
- 阴影仅用于悬浮提示、弹出层和局部工具条。不使用大面积模糊、霓虹描边或持续闪烁。
- 可点击项具备正常、悬停、按下、禁用、键盘聚焦状态；纯装饰不伪装成按钮。

## 4. 地图标记体系

### 4.1 大小设置

| 分类 | 初始逻辑尺寸 | 配置关系 |
| --- | --- | --- |
| 水晶传送 | 24 | 独立倍率 |
| CE | 30 | 事件倍率；高级设置可单独覆盖 |
| FATE | 26 | 事件倍率；高级设置可单独覆盖 |
| 魔法罐 | 28 | 独立倍率 |
| 魔法罐预测 | 32 | 独立倍率 |
| 高塔等地标 | 32 | 独立倍率，不套用水晶倍率 |
| 普通候选 | 直径 13 | 候选大小设置 |
| 关注候选 | 直径 18 | 关注大小设置 |
| 玩家／队友 | 依现有素材校准 | 各自设置 |

纹理外框尺寸相同，不代表肉眼看到的主体一样大。允许对少数素材设置光学补偿系数，但不能依靠放大全部图标解决透明留白问题，也不应在每帧分析纹理像素。

设置页同时展示水晶、CE、FATE、候选和玩家样例。滑块旁提供数值输入、分类重置和全部恢复默认。预览应使用正式地图标记渲染器，避免预览与实机不一致。

### 4.2 悬停动效与命中

- 悬停约 140ms 放大至 1.20–1.35 倍，离开约 180ms 平滑恢复。
- 普通悬停使用轻描边；关注目标使用稳定金色边框。
- 点击反馈采用短暂描边，不使用大幅跳动。
- 预测点可使用低频轻微呼吸，不能让所有候选一起闪烁。
- 提供“减少动态效果”选项，禁用动画后仍保留静态状态区分。
- 提示框延迟约 200ms 显示，并约束在可见区域内。

视觉边界、命中区域与绘制顺序由统一布局计算。命中区域可以有小幅容错，但不能继续使用与尺寸设置无关的固定半径。

相邻标记重叠时只选择一个主悬停对象；优先级、到中心的距离和稳定 ID 共同决定结果。悬停标记最后绘制，采用稳定基础命中区或滞回逻辑，防止放大后悬停对象来回切换。一次点击只能触发一个动作。

帧率无关插值可采用：

```csharp
value += (target - value) * (1f - MathF.Exp(-speed * deltaTime));
```

### 4.3 候选与提示范围

- 普通候选：青色填充、清晰白色细边。
- 关注候选：较大金色圆点与外圈，避免仅靠改变颜色表达关注。
- 已确认宝箱：绿色与宝箱图形；与预测点明确区分。
- 密集时仅为悬停、关注对象显示编号，避免满屏文字。
- 提示扇形采用低透明填充、明确边界。多层叠加限制累计饱和度，不能盖住地形。
- 无有效候选、提示冲突、数据过期使用不同状态，不能统一显示为“0 个”。

## 5. 场景标注

默认标注压缩为两行：

```text
魔法罐候选 #60
312m · ↑18m
```

关注点使用金色，普通候选使用青色或白色。完整名称、轮次、坐标和来源放入详情面板。

- 按“关注目标 > 已确认宝箱 > 最近候选”的优先级进行标签布局。
- 用屏幕矩形检测重叠，轻微纵向错开，并通过短引导线关联实际点位。
- 相机后方和屏幕外标签直接剔除；过密时隐藏低优先级标签。
- 高差未知显示“高差未知”，不能把玩家当前 Y 坐标作为有效地面高度。
- 地面高度查询移出 Draw 流程，通过服务按预算刷新。
- 缓存键包含区域、轮次及候选坐标，不能只使用候选编号。
- 成功结果允许较长缓存；失败结果记录原因并适度重试。换区和数据更新后失效。

## 6. 寻宝、事件与路线详情

### 6.1 寻宝页

从上到下展示当前轮次、提示数量、剩余候选数、当前关注点、提示时间线和操作区。

提示时间线展示方向、距离档位及是否采纳。遇到冲突时保留上一份有效候选结果，并明确说明新提示为何未纳入；最终处理规则必须与 DR 基线核对。

“撤销上一条提示”和“重置本轮”应分开摆放，重置不能与常用导航操作相邻。空状态解释如何获得首条提示。

### 6.2 CE／FATE 事件页

侧栏采用紧凑列表，独立事件窗口保留适合比较的表格。

| 信息 | 展示方式 |
| --- | --- |
| 关注 | 星标按钮，已关注状态明确 |
| 事件名称 | 主文字，长名称悬停展开 |
| 状态 | 报名／准备／战斗／未出现等标签 |
| 触发怪 | 次级文字，等级独立呈现 |
| 地点 | 定位操作，坐标放入提示框 |
| 进度／倒计时 | 仅在数据有效时显示细进度条或时间 |
| 奖励与详细条件 | 行展开或详情区 |

区域列仅在跨区域查询时出现。ID、数据来源等调试信息放入高级详情。当前区域“未出现”与其他区域“状态未知”必须区分。

提供搜索、仅关注和状态筛选，无结果时提供清除筛选入口。窗口变窄时优先隐藏次要列，保留名称与主要操作。

收藏、提醒和定位操作必须对应实际功能与验收记录，不能仅添加看起来可用的控件。事件行使用稳定 ID；过滤结果与事件索引缓存，避免每行每帧扫描全部事件。

### 6.3 路线页

展示当前目标、选用水晶、有效路线长度、导航阶段与停止按钮。预计到达时间没有可靠计算依据时不显示。

| 路线类型 | 绘制 |
| --- | --- |
| 实际步行路径 | 青色实线 |
| 传送段 | 端点间虚线，标注“传送” |
| 传送后路径 | 已计算时才显示较淡实线 |
| 绕行辅助信息 | 可选金色辅助线 |

路线图层默认关闭，用户主动开启。没有实际路径时显示等待／失败原因，不画一条直线冒充已计算路径。

导航状态使用结构化快照，例如目标、阶段、选用水晶、路径、错误、是否可取消；界面不得通过解析中文状态字符串推断流程。

视觉简化后的折线仅用于绘制，不能替换导航服务使用的真实路径。停止按钮需要调用统一取消入口；键盘取消、取消后禁止旧异步任务继续推进等行为需独立验证。

## 7. 怪物警戒与绕行显示

- 未确认视野形状时使用轻量范围提示；有可靠数据时显示视野锥形。
- 与当前路线相关的危险区域才提高透明度，避免地图铺满红色。
- 明显不同高度的区域按可信高度信息隐藏或淡化。
- 数据置信度、校准来源和怪物 ID 仅在调试模式显示。
- 绘制与避让计算读取同一份警戒数据快照，防止画面提示与实际绕行规则不一致。

## 8. 设置界面

采用左侧分类、右侧配置的结构，提供搜索。窄窗口可切换为分类下拉选择。

| 页面 | 内容 |
| --- | --- |
| 外观 | 密度、面板透明度、辅助文字、减少动态效果 |
| 地图标记 | 各类大小、悬停倍率、透明度、真实图标预览 |
| 场景标注 | 标签密度、距离显示、高差显示、避让 |
| 寻宝 | 与 DR 对齐的提示、候选和关注行为 |
| 事件提醒 | 收藏、过滤和已有提醒能力 |
| 导航 | 水晶选择、坐骑、取消、绕行与路线图层 |
| 高级诊断 | IPC、导航状态、地面查询、预测数据、错误记录 |

设置修改即时预览；持久化采用拖动结束保存或防抖保存，避免滑块每帧触发文件写入。提供本页恢复默认，配置迁移保留已有用户偏好。

高级诊断应包含：vnavmesh 可用状态及 Action／Function 检测结果、最近导航阶段和停止结果、地面高度来源／失败原因、预测记录时间／来源／有效性及实际查询状态。允许复制诊断文本，普通界面不暴露这些实现细节。

## 9. 技术拆分建议

以下名称是建议组件，不表示当前工程已经具有这些类型。

| 组件 | 职责 |
| --- | --- |
| `ThemeTokens` | 色彩、间距、圆角、密度等统一参数 |
| `UiStyleScope` | 局部样式 Push／Pop 成对管理，不影响其他插件 |
| `IconButton`／`StatusBadge`／`SectionPanel` | 统一按钮、状态标签、内容分组 |
| `ResponsiveLayout` | 紧凑／展开布局与窄窗口降级 |
| `MapViewport` | 世界坐标转换、缩放、平移、裁剪 |
| `MapMarkerLayout` | 标记尺寸、视觉边界、命中区域及排序 |
| `MapMarkerRenderer` | 统一纹理、候选、关注效果及绘制 |
| `MapInteractionController` | 主悬停对象、点击派发、地图拖动协调 |
| `AnimationStateStore` | 基于稳定 ID 的动画状态与清理 |
| `TooltipController` | 延迟、内容和可见区域约束 |
| `SceneLabelLayout` | 场景标签优先级、重叠检测与避让 |
| `GroundPositionService` | 地面高度查询、缓存、失败重试和失效 |
| `NavigationSnapshot` | 导航服务向界面提供的只读状态 |
| `MarkerPreview` | 复用正式渲染器的设置预览 |
| `UiPreferences` | 视觉配置、默认值与配置迁移 |

标记模型至少包含：稳定键（区域＋类别＋实体）、类别、世界坐标、基础尺寸、视觉边界、命中边界、悬停与选中状态。

与现有代码的职责对应建议：

| 现有文件／模块 | 计划职责 |
| --- | --- |
| `Windows/TreasureMapWindow.cs` | 主窗口组织，逐步委托视口、布局和交互组件 |
| `Windows/MainWindow.cs` | 寻宝详情与提示历史，避免重复维护业务数据 |
| `Windows/CeWatchWindow.cs` | 事件列表／表格与详情 |
| `Windows/ConfigWindow.cs` | 分类设置和图标预览 |
| `Rendering/SceneOverlayRenderer.cs` | 使用场景标签布局及已准备好的地面数据 |
| `Services/NavigationService.cs` | 提供导航快照和统一停止入口 |
| `Services/TreasureTracker.cs` | 候选与提示结果，保持规则独立于界面 |
| `Services/OccultEventTracker.cs` | 事件快照及索引 |
| `Integrations/VNavmeshIpc.cs` | IPC 能力适配，不承担 UI 状态拼装 |
| `Configuration/PluginConfiguration.cs` | 承载或引用带版本迁移的视觉配置 |

实施前核对最新源码，避免按照历史文档推断现状。

### 9.1 渲染与性能约束

- 使用标准 ImGui 控件承担布局、输入和聚焦；DrawList 主要用于地图、图形标记、局部强调与进度显示。
- Draw 流程只读取准备好的快照，不执行网络、文件 IO 或同步寻路／地面查询。
- 复用纹理提供者与缓存，明确资源所有权，不擅自释放借用的纹理。
- 避免每帧大量 LINQ、字符串拼接和临时集合；优化以实际分析结果为依据。
- 动画状态数量有上限，标记消失、换区、窗口关闭和卸载时按生命周期清理。
- 关闭窗口后的必要业务监听与纯视觉更新分开处理，防止关闭 UI 意外停止业务或仍进行无意义渲染。

## 10. 游戏原生地图集成：仅讨论

这里需要区分两件事：独立窗口使用游戏地图纹理，以及把功能直接画到游戏自身的地图窗口。后者是可选的独立阶段。

建议保留独立窗口，同时评估原生地图作为第二种展示入口。候选、预测、事件与导航快照共享，视口与输入分别适配。

需要先验证：

1. 当前客户端和 Dalamud 版本下，地图 Addon 的地图 ID、缩放、平移、裁剪和生命周期获取方式。
2. 原生地图已有水晶、CE／FATE 等标记如何复用或避免重复覆盖。
3. 新增导航点击与原生地图拖动、旗标、右键菜单的冲突处理。
4. 地图打开／关闭、区域切换、UI 缩放和插件卸载后的节点清理。
5. 接口变化或适配失败时如何退回独立窗口。

首个验证范围宜仅覆盖候选与关注点显示。完成坐标与交互验证后，再讨论路线和更多操作。具体 Addon API 与实现方式须届时核实，本文不承诺直接可用。

## 11. 分阶段实施

| 阶段 | 交付 | 验收重点 |
| --- | --- | --- |
| P0 基线记录 | 固定区域、窗口尺寸、UI 缩放的当前／DR 截图与行为记录 | 区分视觉差异与功能缺陷 |
| P1 视觉基础 | 主题、稳定标题区、独立图标尺寸、统一命中区域 | 图标调节正确，点击准确，不增加常驻状态行 |
| P2 地图与场景 | 候选样式、悬停动效、标签避让、高差可信展示 | 与 DR 视觉对照，未知高差不伪装成零 |
| P3 信息组织 | 详情侧栏、事件表格、分类设置、真实预览 | 紧凑与展开布局可用，用户设置正确迁移 |
| P4 导航可视化 | 结构化阶段、真实路线、警戒显示 | 视觉路径与实际导航数据一致，完整功能回归 |
| P5 可选探索 | 原生地图适配验证 | 另行确定范围后实施 |

优先实施 P1 与 P2，直接解决图标观感、尺寸控制、候选显示和地图拥挤问题。每阶段保留可对照截图与明确的游戏内验证结果，不把编译成功当作交互已通过。

## 12. 验收清单

### 视觉与交互

- [ ] 100%／125%／150%／200% UI 缩放下无重复缩放、截断和错位。
- [ ] 最小支持窗口尺寸下仍能访问主要操作，长中文名称可查看全文。
- [ ] 传送、CE／FATE、其他地标倍率互不误用，预览与正式地图一致。
- [ ] 放大后点击区域正确；重叠标记只触发一个目标，悬停不抖动。
- [ ] 拖动地图、操作工具条和点击标记不会互相误触。
- [ ] 减少动态效果开启后状态仍清楚，工具提示不越界。
- [ ] 普通、关注、预测与确认标记无需仅凭颜色区分。
- [ ] 常规取消不会留下永久状态行或造成地图上下跳动。

### 数据真实性与功能一致性

- [ ] 有效、过期、查询中、失败和无数据状态分别验证。
- [ ] 未知高度显示未知，真实零高差仍可正确显示。
- [ ] 候选筛选、冲突处理、撤销、轮次与定制路线逐项对照 DR。
- [ ] 水晶点击传送、传送后上坐骑、CE／FATE 导航均在游戏内验证。
- [ ] WASD／Esc 与停止按钮可以取消相应导航流程，旧任务不会自动恢复。
- [ ] vnavmesh 不可用或未就绪时操作状态明确，失败原因可查询。
- [ ] 未计算路径不显示为有效路线；绘制简化不改变寻路输入。

### 生命周期与性能

- [ ] 换区、地图切换、窗口关闭和插件卸载无残留标记或异常。
- [ ] 样式作用域平衡，不污染游戏内其他插件界面。
- [ ] 配置迁移保留已有偏好，重置只影响声明的范围。
- [ ] 对比相同场景下改动前后的绘制耗时与内存分配，记录实测结果。
- [ ] 高密度候选、事件和标签下仍可操作，无每帧文件／网络／同步查询。

## 13. 本文交付范围

本次仅创建这份设计文档。未修改插件代码、配置或构建产物，未进行游戏内测试。后续实施应以本文、项目计划和 DR 行为对照共同确定验收范围；原生地图集成继续保留为讨论项。
