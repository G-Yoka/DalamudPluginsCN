# 新月罗盘（CrescentCompass）独立插件项目计划

日期：2026-09-09  
状态：实现中；P0、P1 已完成，P2 已接入提示闭环、原生地图及动态事件、缩放拖动、玩家／相机／队友标记、本机魔法罐预告、真实宝箱关联和基础场景标注，等待游戏内功能复测。  
项目目录：`E:\01_Dev\Projects\DailyRoutines.Dev\CrescentCompass`  
迁移来源：`../OccultPotTreasureFinder`（现有界面名称为“罐子宝箱速寻”，即用户所称“罐子宝箱速查”）。

## 1. 名称与目标

独立插件正式命名为 **新月罗盘**，英文名及工程标识为 **CrescentCompass**。

“新月”对应新月岛南北部，“罗盘”对应寻宝定位、事件查询和地图导航。现有模块已包含 CE 关注、触发怪速查、怪物警戒与导航，新的名称为这些能力提供统一入口，同时保留“撒娇罐寻宝”作为核心功能名称。

| 标识 | 约定 |
| --- | --- |
| 中文显示名 | 新月罗盘 |
| 英文名称／清单 InternalName | CrescentCompass |
| 程序集／根命名空间 | CrescentCompass |
| 主程序集 | `CrescentCompass.dll` |
| 主命令 | 拟用 `/crescent`，实现前检查占用 |
| 兼容快捷命令 | 拟用 `/potfind`；检查占用后注册，不接管旧 `/pdr potfind` |
| 一句话介绍 | 新月岛寻宝、事件关注与地图导航助手。 |

目标是在 **不安装、不启用 DailyRoutines** 的环境下，由 Dalamud 直接加载插件，完整承接当前模块的功能。这里的独立插件是 FFXIV／Dalamud 插件。

本轮交付为项目命名和迁移计划；不把当前 DR 模块 DLL 视为独立插件产物。旧目录及其计划保留为实现参考与回退基线，新工程后续不通过跨目录源码链接依赖旧项目。

## 2. 当前实现与迁移基线

以下结论来自本地源码及工程配置检查，不代表已经完成独立构建或新的游戏内验证。

| 现有文件 | 内容及迁移价值 |
| --- | --- |
| `OccultPotTreasureFinder.Local/OccultPotTreasureFinder.Local.csproj` | 使用 `Dalamud.CN.NET.Sdk/15.0.0`、`net10.0-windows`；引用 DR Dev 目录中的三个 DLL，关闭插件打包 |
| `src/OccultPotTreasureFinder.cs` | `ModuleBase` 入口、配置、聊天监听、场景标注、实例状态、传送和导航；是主要解耦对象 |
| `src/OccultPotTreasureFinder.CeWatch.cs` | CE 关注窗口、搜索、收藏、提醒和地图定位；依赖 DR 窗口宿主及 OmenTools 服务 |
| `src/PotOverlay.cs` | 地图绘制、缩放和标记；依赖 OmenTools 的 `ImGuiMapRenderer` |
| `src/PotPrediction.cs`、`src/PotCandidateCatalog.cs` | 提示解析、候选筛选和南北岛分轮次坐标库 |
| `src/MagicPotForecast.cs` | 众包快照与魔法罐时间预测逻辑 |
| `src/NorthHornAggroAvoidance.cs`、`src/AggroMonsterKnowledge.cs` | 警戒区域、绕行计算和怪物资料／校准模型 |
| `src/CeSpawnCatalog.cs`、`src/OccultEventRewardCatalog.cs` | CE、触发怪与事件奖励资料 |
| `tests/OccultPotTreasureFinder.Tests` | 已有可独立执行的纯逻辑测试工程，可作为迁移回归基线 |
| `TESTING.md`、`THIRD_PARTY_NOTICES.md` | 游戏内验收记录及第三方来源，迁移时一并继承与更新 |

旧计划记载北部定位已验证，CE 增量构建通过且 18 组测试通过；导航、生命周期等仍有游戏内待测项。新项目必须重新记录自身测试结果，不直接继承“已通过”状态。旧文档与源码发生差异时，先记录当前源码行为，再通过测试确定迁移基线。

## 3. 首个完整独立版本的范围

以当前模块功能等价迁移为目标，可分阶段实现，但仅完成基础寻宝时不能宣称完整迁移完成。

| 功能 | 独立版本要求 |
| --- | --- |
| 撒娇罐寻宝 | 南北岛、首次与第二次机会、方向与距离筛选、冲突保留、撤销、重置、暂停和恢复 |
| 真实宝箱关联 | 区分预测候选与本轮发现的宝藏；普通铜银箱不进入魔法罐候选库 |
| 地图浮窗 | 原生地图底图、缩放、尺寸及透明度、玩家／相机／队友、水晶、FATE／CE 和两歧塔入口 |
| 场景标注 | 候选、关注点、真实宝箱及怪物警戒；保留距离、数量、战斗隐藏和高度过滤设置 |
| 魔法罐预告 | 当前实例只读众包基准、本机观察校正、提前五分钟提示；过期和不可用状态明确展示 |
| CE 关注 | 现有南北岛 30 个 CE、10 项触发怪区域，中英文搜索、收藏、地图旗标、进岛提示和去重提醒 |
| 怪物警戒 | 保留区域资料、感知类型、距离与视野模型及调试窗口；本地自动校准默认关闭 |
| 点击导航 | 保留近距离直达、亚返回、水晶选择、传送后骑乘与前往事件、动态绕行、重试和取消 |
| 配置与入口 | 独立设置窗口、主命令、Dalamud 设置入口及独立保存 |

vnavmesh 是可选导航依赖；未安装、未启用、IPC 不兼容或网格未就绪时，寻宝、地图、CE 查询和提醒仍可使用，导航入口解释不可用原因。众包服务不可用时继续使用本机观察，不能阻塞插件加载或绘制。

不在本次范围：全图自动巡回、自动询问和开箱、自动切辅助职、新增地区、校准数据上传、服务端搭建及公开插件仓库发布。首版以现有国服中文提示为基线，不因 CE 支持中英文搜索而声称已支持所有客户端语言。

## 4. DR 依赖替换方案

最终工程不引用 `DailyRoutines.dll`、`DailyRoutines.Common.dll`，也不从 DR 安装目录借用 `OmenTools.dll`。为避免隐藏的全局服务依赖，本计划选择将所需 OmenTools 能力替换为插件自己管理的最小实现，而非整体搬入其服务框架。

| 当前依赖 | 目标实现 | 关键验证 |
| --- | --- | --- |
| `ModuleBase`、`ModuleInfo`、`Init/Uninit` | 独立 `IDalamudPlugin` 入口、插件清单及 `Dispose` | Dalamud 直接识别、加载与卸载 |
| `ModuleConfig`、`FinderConfig.Load/Save(this)` | 独立版本化配置，通过插件配置接口保存 | 无 DR 时可保存、重启恢复及迁移 |
| `ManagerHost`、模块 `Overlay`、`WindowManager.PostDraw` | 插件持有的 `WindowSystem` 和 UiBuilder 绘制生命周期 | 地图、CE、设置、调试窗口独立关闭及清理 |
| `CommandManager.AddSubCommand` | Dalamud 命令服务注册独立命令 | 无 `/pdr` 前缀；卸载后注销 |
| OmenTools `I…Instance()`、全局别名与 `FrameworkManager` | 注入 Dalamud 服务并显式订阅更新事件 | 去除静态宿主假设；无重复订阅 |
| `ImGuiMapRenderer` | 插件内地图投影、纹理与交互组件 | 世界坐标、地图坐标、缩放、透明度和图标回归 |
| `ZoneIndicatorRenderer`、射线等助手 | 独立场景渲染及地形查询适配层 | 镜头后方裁剪、高差、卸载清理和性能 |
| `GameState`、`LuminaGetter`、对象及事件助手 | Dalamud 数据服务与隔离的游戏状态读取层 | 实例 ID、区域、CE／FATE 状态有效性 |
| `UseActionManager`、传送和游戏交互助手 | 隔离的动作／传送适配器，按目标客户端校验接口 | 下坐骑、亚返回、传送、校准动作观测及生命周期 |
| `vnavmeshIPC` 包装 | 插件自己的 IPC 适配器 | 可用性检测、异步取消、路径控制权和版本不兼容回退 |
| 通知、音效、语言及 ImGui 扩展 | Dalamud 通知与界面接口及必要的本地辅助函数 | 提醒去重、错误可见、无 DR 语言管理器引用 |

以上是职责替换设计，具体 API 签名在实现阶段按本机 Dalamud／FFXIVClientStructs 引用核对。当前 SDK 15 与 .NET 10 仅作为已观察到的起始基线，不声称它们是未来发布时的最新版本。特别验证聊天事件时机，保证能接收到合法原始提示且不屏蔽原消息。

原生指针只在有效生命周期内读取，复制成快照后交给逻辑层；切区、登出和卸载取消异步任务并丢弃旧代次结果。动作观测若需 Hook，应仅在所需功能启用时持有并释放；失败时关闭对应校准功能，不能使基础寻宝无法加载。

## 5. 目标工程结构

```text
CrescentCompass/
  PROJECT_PLAN.md
  README.md
  TESTING.md
  THIRD_PARTY_NOTICES.md
  LICENSE
  src/
    CrescentCompass/
      CrescentCompass.csproj
      CrescentCompass.json
      Plugin.cs
      Configuration/
      Services/             # Dalamud 服务、区域与对象快照
      Windows/              # 地图、设置、CE 关注、调试
      Rendering/            # 地图与场景标注
      Integrations/         # vnavmesh、众包客户端
      GameInterop/          # 动作、传送、原生读取与射线
    CrescentCompass.Core/
      CrescentCompass.Core.csproj
      Treasure/
      Events/
      Navigation/
      Monsters/
      Data/
  tests/
    CrescentCompass.Core.Tests/
  artifacts/
```

此目录树为拟建结构，本轮只创建计划。Core 仅依赖 .NET，不引用 Dalamud、DR 或 OmenTools，容纳可测试的数据、预测、状态和几何算法。插件层负责输入快照、界面和游戏交互；第三方集成通过边界接口进入，便于模拟断连与失败。

迁移源码时保留来源说明并改用新命名空间，禁止新工程 `Compile Include` 链接旧模块目录。构建设置使用可配置的 Dalamud 开发环境路径，不保留 DR Dev 目录或开发者私有绝对路径。插件清单与打包方式按照实际 SDK 验证，产物应包含可加载程序集、清单及必要资源。

## 6. 独立配置

1. 使用独立配置版本保存 CE 收藏、通知、地图透明度、导航阈值、警戒参数、校准样本和调试开关。
2. 首次使用由新插件建立默认配置，未知字段由序列化层忽略。
3. 运行中的导航、临时宝箱指针、提示会话、当前实例快照和通知去重状态始终从当前游戏重新建立。
4. 切换使用时关闭 DR 中的旧模块，避免重复窗口、提醒和导航竞争。DR 其他模块可继续运行。

命令帮助统一使用新名称及 `/crescent`；拟保留 `reset`、`pause`、`resume`、`next`、`map`、`cancel`，新增 `config` 和 `ce` 窗口入口。独立窗口 ID 使用 `CrescentCompass` 前缀。

## 7. 实施阶段与完成条件

### P0：冻结基线与依赖盘点

- [x] 记录旧源码快照或提交、SDK／游戏版本、现有测试结果和游戏内已验证范围；见 `BASELINE.md`。
- [x] 逐项盘点直接引用、全局 using、扩展方法、原生交互和 IPC，形成替换清单。
- [x] 核实第三方来源与待继承资料，区分既有缺陷和迁移回归。

完成条件：每项 DR／OmenTools 调用有明确替代归属，当前行为和未验证事项有记录。

### P1：独立宿主与配置

- [x] 建立入口、服务注入、清单、构建与打包；完成设置窗口、命令、保存和卸载。Dalamud 日志已确认独立加载与卸载成功。
- [x] 建立 Core 与测试工程，迁移数据及纯逻辑并执行原测试基线；18 组测试通过。
- [x] 建立独立配置 schema、字段校验和版本升级。

完成条件：无 DR 的开发环境中可构建，在未安装 DR 的 Dalamud 环境中能加载、打开设置和保存；Core 测试无需游戏程序集。

### P2：寻宝与显示闭环

- [ ] 接入聊天提示、区域生命周期和真实宝箱关联。代码已完成：发现提示后只关联 35m 内最近的有效 Treasure，并清理第二轮／消失对象；等待真实宝箱游戏内验证。
- [ ] 替换地图底图、坐标转换、场景标注及角色标记。原生底图已实机显示；默认标记、缩放拖动、动态事件、相机、队友及独立 `WorldToScreen` 标签已实现并等待实机复测，地面高度解析仍待迁移。
- [x] 恢复南北岛首次／第二次机会、撤销、暂停、重置与关注点操作。

完成条件：无 DR、无 vnavmesh 时，真实提示仍能驱动“询问 → 缩小候选 → 地图／场景定位 → 发现／结束”流程。

### P3：事件、预告与怪物警戒

- [ ] 迁移 CE 收藏、触发怪速查、名称解析、旗标及去重提醒。已实现 30 项 CE／10 项触发怪列表、客户端名称、实时状态、搜索、区域过滤和收藏持久化；旗标与提醒待迁移。
- [ ] 迁移实例隔离、只读众包、本机预告校正与两歧塔入口。实例读取、众包查询、顶部状态、本机校正和地图预告已实现并等待实机复测；两歧塔点击入口随导航阶段迁移。
- [ ] 迁移怪物快照、警戒渲染、可选校准和调试窗口。

完成条件：南北岛资料、提醒及显示与基线一致；网络不可用、实例切换和观测失败有可验证的回退。

### P4：导航与游戏交互

- [ ] 接入独立 vnavmesh IPC，迁移路线选择、亚返回、传送、坐骑和到达流程。已实现直达／亚返回传送／步行水晶传送三路真实路径预计用时比较、10 秒传送收益迟滞、100m 水晶营地保护、本机动作耗时学习、可取消异步寻路、水晶真实对象交互、传送落地续航和移动中上坐骑；等待完整实机回归。
- [ ] 迁移动态绕行、异步路径计算、停滞恢复和有限重试。已加入取消后重启时的实时起点复核、有限重新规划和已通过路径前缀裁剪，等待实机验证。
- [ ] 验证重复点击、取消命令、WASD／Esc、战斗、离区及卸载的取消行为。代码已实现同目标再次点击、`/crescent cancel`、WASD／Esc、离区和卸载取消，等待实机验证。
- [ ] 保证只停止本插件拥有的路径；外部接管或无法确认所有权时退出自身流程，避免误停其他插件导航。

完成条件：两段导航在南北岛均完成实际验证；可选依赖缺失和中途失效不会拖垮其他功能。

### P5：完整回归与交付

- [ ] 完成下列验收矩阵与旧 `TESTING.md` 中所有适用场景。
- [ ] 审查程序集依赖、源码引用、构建路径和包内容，排除残留 DR／OmenTools 运行依赖。
- [ ] 完成 README、安装与旧版切换说明、版本基线、已知问题、第三方声明及许可证文件。
- [ ] 交付源码、插件包、构建记录、SHA-256 和游戏内验收记录。

完成条件：独立加载、功能迁移、卸载清理和可复现交付均通过；公开发布另行处理。

## 8. 验收矩阵

| 验收项 | 方法与通过标准 |
| --- | --- |
| 构建独立性 | 在没有 DR Dev DLL 的环境构建；工程、依赖图与包内容不含 DR／Common／OmenTools 引用，不链接旧项目源码 |
| 运行独立性 | 未安装 DR 时由 Dalamud 加载；命令、设置、地图、寻宝、CE 和警戒可用 |
| 纯逻辑回归 | 重跑现有解析、几何、会话、预告、CE、校准与绕行测试，补充迁移引入的边界案例 |
| 提示与候选 | 八方向、距离边界、冲突与撤销；两岛首次／第二次机会分离，普通箱不混入 |
| 游戏内显示 | 地图旗标、世界投影、高差、玩家与队友、缩放和透明度实际核对 |
| CE 与实例 | 收藏重启保留；进岛及出现提醒去重；换实例不沿用旧记录；未知冷却保持未知 |
| 网络降级 | 断网、超时和迟到响应不阻塞界面、不跨实例污染，本机观察继续工作 |
| 导航 | 南北岛近距离直达及两段路线、不可达、停滞、动态障碍、手动取消、IPC 中断 |
| 配置持久化 | 空配置、损坏文件、未知字段和版本升级；设置重启后保持一致 |
| 生命周期 | 至少连续三次加载／卸载，另测离区、登出与换角色；无重复订阅、残留窗口／标注或后台移动 |
| 并用与性能 | DR 开启但旧模块关闭时可用；导航不误停他人路径；绘制不做文件／网络 IO，扫描节流且缓存有界 |

构建通过和纯逻辑测试不能代替游戏内验收。所有记录需附新插件版本、客户端／Dalamud 版本、场景、结果与未覆盖项。

## 9. 主要风险与处理

- **地图和场景渲染替换工作量较大**：先验证单点投影、缩放及裁剪，再迁移全部标记，避免一次性重写后难以定位偏移。
- **原生接口随客户端变动**：集中到 GameInterop，初始化验证失败时禁用受影响能力并记录原因，不以静默异常掩盖失效。
- **导航 IPC 没有可靠的全局所有权保证**：维护本插件请求代次和路径状态，验证外部接管场景；无法满足时作为导航阶段阻塞项记录。
- **旧测试记录不完整或文档与实现不一致**：以冻结源码及实测建立基线，保留未验证标签，不把旧计划的勾选自动视为新项目通过。
- **第三方代码和数据来源**：继承旧 `THIRD_PARTY_NOTICES.md` 的来源与提交信息；实施时核实实际采用内容及许可证，补齐 CE 资料的来源记录。改名与解耦不删除既有署名。

## 10. 最终交付判定

只有当 `CrescentCompass` 能在没有 DR 的环境中独立构建与加载，完成本计划内的功能等价迁移及实际验收，并提供完整可复现的源码和插件包时，才标记为“独立插件迁移完成”。

当前已完成：名称与迁移边界、P0／P1、独立 Dalamud 插件宿主、独立配置、Core 数据／算法迁移、聊天提示状态机、候选坐标图、CE 速查与收藏入口，以及场景标签和真实宝箱关联。
